import { PRUFEN } from "../constant/constant";

const initialState = {
  prufen: false;
}




export function reducer(state = initialState, action) => {
  switch(action) {
    case PRUFEN: 
        return state 
      default
        return state

  }
}